# Pull Request

Request to merge the code onto the required branch.

for example, If I am adding a feature of locking unlocking the screen and has written a block of code there. Now I will create a pull request for that feature. and anyone can comment on the feature and discuss. 

After the pull request is accepted, the extended branch is merged. 

Now I am free to delete the feature branch.

If the main branch is authenticated

# Delete a Branch
git branch -d branch_name
# Pull CHanges from github to the local repository